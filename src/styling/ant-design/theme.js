import { theme as AntTheme } from 'antd'

export const theme = {
    algorithm: AntTheme.darkAlgorithm,
    token: {
        colorPrimary: '#646cff',
        borderRadius: 16,
        fontFamily: "'Quicksand', sans-serif",
    },
}
